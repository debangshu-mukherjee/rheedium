Test Reference
==============

Validation and regression tests for Rheedium, documented as a reference
of *what the library guarantees and how each guarantee is checked*. Each
``Test`` class links back to the source symbol it validates via ``:see:``
(bidirectional with the API Reference).

.. toctree::
   :maxdepth: 1

   audit
   inout
   plots
   procs
   recon
   simul
   tools
   types
   ucell
   core
